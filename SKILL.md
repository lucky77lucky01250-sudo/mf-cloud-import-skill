---
name: mf-cloud-import
description: |
  領収書・請求書の画像(JPG/PNG)やPDFを読み取り、ファイルのリネームとMFクラウド仕訳インポート用CSV・Excel管理ファイルを自動生成するスキル。
  フォルダ内の領収書データを一括処理し、日付_店舗名の命名規則でリネーム、勘定科目の自動判定、MFクラウド形式のCSV出力まで行う。
  さらに、MFクラウドに連携した銀行口座・クレジットカードの明細から仕訳を登録する手順も含む（末尾の追補セクション）。
  このスキルは以下のキーワードで発動すること：領収書、請求書、レシート、仕訳、経理、MFクラウド、マネーフォワード、MoneyForward、勘定科目、取込、インポート、経費精算、帳簿、連携、カード明細、未払金、残高試算表。
  ユーザーがフォルダ内に領収書や請求書があり「整理して」「取り込んで」「仕訳して」などと言った場合も発動する。
  「今月の仕訳」「未仕訳を登録して」など、連携明細の処理を求められた場合も発動する。
---

# MFクラウド取込スキル

領収書・請求書ファイル（JPG/PNG/PDF）を読み取り、以下の処理を自動で行います：

1. **ファイルリネーム** — 日付_店舗名_内容_金額円 の命名規則に統一
2. **勘定科目の自動判定** — キーワードルールで自動振分
3. **MFクラウド取込用CSV生成** — Shift-JIS形式でそのままインポート可能
4. **Excel管理ファイル生成** — ルール設定・データ一覧・MF出力の3シート構成

---

## ★★★ ファイル数の制限（必ず守ること） ★★★

**1回のセッションで処理するのは最大30ファイルまで。**
- 30ファイルを超える場合は、必ずユーザーに分割処理を提案すること
- 「ファイルが○個あります。30個ずつに分けて処理しますか？」と確認する
- 分割する場合はファイル名の辞書順で先頭30個を処理し、残りは次のセッションで処理する
- **この制限を無視して一括処理してはならない。精度が著しく低下する。**

---

## 処理フロー

### Step 1: 事前確認

ユーザーに以下を確認する：

- 対象フォルダのパス（ワークスペースフォルダ or 指定パス）
- 作成ファイルに目印を付けるか（デフォルト: ファイル名先頭に「●」）
- 勘定科目ルールのカスタマイズ要望があるか

### Step 2: ファイル一覧取得

対象フォルダ内のJPG/PNG/PDFファイルを一覧取得する。
画像ファイル数とPDFファイル数を報告する。
**30ファイルを超える場合は分割処理を提案する。**

### Step 3: ファイル読み取り（★最重要ステップ）

**1ファイルずつ必ずReadツールで読み取る。推測で情報を埋めてはならない。**
**Agentツールでの並列処理は禁止。必ず自分自身で1枚ずつ読むこと。**

#### 画像の向きに関する注意（★非常に重要）
- 領収書・レシートの画像は**横向き・逆さま・斜めの場合がある**
- 向きが正しくなくても、見えている文字は読み取れる。向きを理由に「不明」にしてはならない
- 縦書きの領収書もある。右から左に読むこと

#### 画像ファイル（JPG/PNG）の読み取りルール

Read ツールで画像を1枚ずつ読み取り、以下の4項目を抽出する。

##### ① 日付の抽出（優先順位順）
1. 「発行日」「利用日」「ご利用日」「取引日」「日付」ラベルの横にある日付
2. レシート上部に印字された年月日（例: 2024年8月16日、2024/08/16、R6.8.16）
3. 領収書の「上記の金額を領収しました」の付近にある日付
4. **注意**: 領収書番号（No.）や伝票番号を日付と間違えないこと
5. **月日4桁（MMDD）で表す**: 2024年8月16日 → 0816
6. **和暦変換**: 令和N年 = N+2018年（例: 令和6年=2024年、令和8年=2026年）
7. 日付が見つからない場合のみ「0000」とする

##### ② 店舗名の抽出（★間違いが多い項目）
1. レシート/領収書の**最上部**にある店舗名・会社名（ロゴの下の名称）
2. 請求書は**差出人（サービス提供元）** の会社名を使う

**★絶対に守るべきルール：**
- **「請求先」「宛先」「〇〇様」「〇〇御中」に書かれている名前は自社名（受け取り側）であり、店舗名ではない**
- 具体例: 請求書に「請求先: multi-solution」と書いてあっても、multi-solutionは店舗名ではない。差出人の方が店舗名
- 「領収書」「請求書」というタイトル自体を店舗名にしない
- 電話番号やインボイス番号を店舗名にしない
- 店舗名が完全に読めない場合のみ「不明」とする。部分的に読めればそれを使う

##### ③ 金額の抽出
1. **「合計」「ご請求金額」「請求金額」「お支払い金額」「税込合計」** の横の金額（税込の最終金額）
2. 「小計」ではなく**合計**を使う。お釣りや消費税額だけの数字と間違えないこと
3. ¥マーク、カンマ、「円」を除去して数字のみにする
4. 金額が読み取れない場合のみ「金額不明」とする

##### ④ 内容（摘要）の抽出
1. 購入品目・サービス名を簡潔に（例: コーヒー代、飲食代、書籍代、サーバー利用料）
2. レシートなら主な商品名（例: コーヒー、ランチ）
3. 請求書なら件名・品名欄（例: Web制作費、応援会費）
4. 内容が不明でも、店舗の業種から推測する（飲食店→飲食代、書店→書籍代）

#### PDFファイルの読み取りルール

Read ツールでPDFを1ファイルずつ読み取る。

**PDFはテキストデータがそのまま読めるため、画像よりも正確に読めるはず。PDFで「不明」にするのは最終手段。**

1. 請求書PDFの日付は「発行日」「請求日」を使う（「支払期限」「お振込期限」は使わない）
2. 請求書の金額は「ご請求金額」「合計金額（税込）」「請求金額」を使う
3. **差出人と宛先を絶対に間違えない:**
   - 差出人（＝サービス提供元・お金を受け取る側）→ これが店舗名
   - 宛先・請求先（＝自分・お金を払う側）→ これは店舗名ではない
   - 「請求先: ○○」の○○は宛先であって店舗名ではない
4. サブスク系PDF（月額サービス）は「利用期間」から月を特定する

#### クレジットカード明細の判定
以下に該当する場合はクレジットカード明細として判定し、仕訳対象から除外する：
- 「ご利用明細」「カード明細」「ご利用代金明細」等のタイトル
- 三井住友カード、楽天カード、JCB、アメックス等のカード会社名
- 複数の取引が一覧になっている

#### ★ 検証ステップ（読み取り後に必ず実行）

全ファイルの読み取り完了後、「不明」が含まれるファイルを一覧化し、**もう一度Readツールで再読み取りする**。
2回読んでも不明な場合のみ、最終的に「不明」とする。

**自己チェック項目:**
- 日付が妥当か？（未来の日付になっていないか）
- 店舗名が「請求先」「宛先」の名前になっていないか？
- 金額が0円や異常に大きい金額になっていないか？
- 内容が具体的か？（「不明」をできるだけ減らす）

**抽出データの形式:**
```
(日付, 店舗名, 金額, 内容, 勘定科目, 税区分, 摘要, 支払方法)
```

### Step 4: 勘定科目の自動判定

`references/kamoku_rules.md` に定義されたキーワードルールに基づいて勘定科目を判定する。
ユーザーがカスタムルールを指定している場合はそちらを優先する。

判定ロジック:
1. 店舗名・摘要テキストに対してキーワードマッチングを行う
2. 最初にマッチしたルールの勘定科目を適用
3. どのルールにもマッチしない場合は「雑費」を適用し、要確認マークを付ける

ユーザーが勘定科目を口頭で修正した場合は、`references/kamoku_rules.md` にルールを追記して永続的に保存する。

### Step 5: ファイルリネーム

命名規則: `MMDD_店舗名_内容_金額円.拡張子`
- 例: `0816_シアトルズベストコーヒー_コーヒー代_340円.JPG`
- 例: `0901_Conoha_サーバー利用料_1452円.pdf`
- 例: `0929_Amazon_日用品購入_3280円.pdf`
- 例: `1019_オンラインサロン_月会費_2905円.pdf`
- 金額にはカンマを入れない（ファイル名のため）
- 同一日付・同一店舗で複数ある場合は `_請求書` `_領収書` `_明細` 等のサフィックスを付ける
- 作成ファイル（Excel/CSV）にのみ目印プレフィックスを付ける（デフォルト: ●）
- **不明な項目がある場合**: わかる情報だけ入れる（例: 日付不明なら `0000_店舗名_内容_金額円`）

衝突回避のため、一旦テンポラリ名（__tmp__プレフィックス）にリネームしてから最終名に変更する。

### Step 6: Excel管理ファイル生成

openpyxl を使用して3シート構成のExcelファイルを作成する。

#### Sheet 1: 勘定科目ルール
- キーワード（黄色セル、ユーザー編集可能）
- 勘定科目
- 補助科目
- 税区分
- 摘要テンプレート

#### Sheet 2: 仕訳データ一覧
- No, 種別(JPG/PDF), 取引日, 店舗名, 金額, 内容, 勘定科目, 税区分, 摘要, 支払方法
- 合計行にSUM数式を使用
- 要確認行は黄色ハイライト
- カード明細はグレーハイライト

#### Sheet 3: MFクラウド出力
MFクラウドのインポート形式（27列）に変換したデータ。

**MFクラウドCSVの列構成:**
```
取引No, 取引日, 借方勘定科目, 借方補助科目, 借方部門, 借方取引先,
借方税区分, 借方インボイス, 借方金額(円), 借方税額,
貸方勘定科目, 貸方補助科目, 貸方部門, 貸方取引先,
貸方税区分, 貸方インボイス, 貸方金額(円), 貸方税額,
摘要, 仕訳メモ, タグ, MF仕訳タイプ, 決算整理仕訳,
作成日時, 作成者, 最終更新日時, 最終更新者
```

仕訳の基本パターン:
- **借方**: 勘定科目（経費）、税区分、金額
- **貸方**: 現金 or 未払金（支払方法に応じて）、対象外、金額
- **借方取引先**: 空欄（免税事業者が多い想定のため）
- **摘要**: 「店舗名 内容説明」

数式の再計算は `scripts/recalc.py`（xlsxスキルに含まれる）を使用する。
recalc.py が見つからない場合は LibreOffice で直接再計算する:
```bash
libreoffice --headless --calc --convert-to xlsx output.xlsx
```

### Step 7: MFクラウド取込用CSV生成

- **文字コード**: Shift-JIS（MFクラウドの仕様）
- **形式**: カンマ区切りCSV
- Excel Sheet 3 と同じデータをCSVとして出力
- ペアドキュメント（請求書/領収書）は1件にまとめる
- カード明細・補助金報告書等は仕訳対象外として除外

### Step 8: 完了報告

以下を報告する:
- リネームしたファイル数（JPG + PDF）
- 仕訳件数と合計金額
- 要確認項目があればリスト
- Excel・CSVファイルへのリンク

## 重要な注意事項

- 金額の読み取りが不確実な場合は必ず「要確認」マークを付ける
- 三井住友カード等のクレジットカード明細は複合明細のため、仕訳対象から除外する
- 同一取引の請求書/領収書ペアは、CSVでは1件にまとめる（Excelには両方記載）
- 消費税区分は基本「課税仕入 10%」、会費等は「対象外」
- pip install が必要な場合は `--break-system-packages` フラグを付ける
- **読み取り精度を最優先すること。不明を安易に使わず、1ファイルずつ丁寧に確認する**
- **Agentツールによる並列読み取りは禁止。精度が大幅に低下するため。**

---

# 【追補】銀行・カード連携から仕訳する場合

領収書ではなく、MFクラウドに連携した銀行口座・クレジットカードの明細から仕訳を登録する手順。
実運用で判明した仕様と落とし穴をまとめる。

## 画面の使い分け

| 画面 | パンくず | 用途 |
|---|---|---|
| 連携サービスから入力 | 自動で仕訳 > 連携サービスから入力 | 未仕訳の明細を登録する（作業画面） |
| 仕訳帳 | 会計帳簿 > 仕訳帳 | 登録済みの結果を見る・修正する |

## ★複数カードが混ざる問題（最重要）

同一IDで複数枚のカードを持っている場合、連携すると**事業用も個人用も全部取り込まれる**。

- **分離方法**：「連携サービスから入力」画面の**2つ目の絞り込み**でカード名を選ぶ。
  1つ目の絞り込みは連携サービス（＝カード会社）単位なので分離できない
- 絞り込んだ状態のURLはそのままブックマークできる
- **取り込まれただけでは帳簿に影響しない。**「登録」を押して初めて仕訳になるので、
  個人用が混ざっていても誤登録しない限り安全。ユーザーが不安を示したらこの点を先に伝える

## カード払いの経費は貸方を「未払金」で統一する

- 利用時：`経費 / 未払金`
- 引落時：`未払金 / 普通預金`

**事業主借と混ぜてはいけない。** 利用を「経費/事業主借」、引落を「未払金/普通預金」で計上すると、
未払金に借方残が積み上がって貸借が崩れる。

- 連携済みカードは「データ連携 > 登録済一覧 > 科目設定」で貸方科目が自動割当される（既定は未払金）。
  カードごとに補助科目が付くため、**引落側の仕訳にも同じ補助科目を入れる**と補助元帳で突合できる
- カードで払った私的な支出も「対象外」にしない。**事業主貸 / 未払金** で計上する。
  対象外にすると引落額と未払金が合わなくなる

## 検算のしかた

「会計帳簿 > 残高試算表」で未払金を見る。`貸方（利用）− 借方（引落）＝ 未払残高`。
締め日までの利用合計と引落額が一致すれば、取り込みと計上が正しい。**登録後は必ずこれで検算する。**

## MFクラウドのUIの癖（ブラウザ操作時）

- **「一括登録」は確認ダイアログが出て自動操作が固まる。行ごとの「登録」ボタンを使う**
- **勘定科目のドロップダウンは、開いて科目名を入力し Enter で確定するのが確実。**
  候補リストの表示位置が上下に揺れるため、座標クリックは外れやすい
- 1行登録すると下の行が繰り上がる。**同じ座標を連打すれば上から順に処理できる**
- ページを再読み込みすると、未登録行に設定した勘定科目は**全部リセットされる**。
  「科目を設定 → その行を登録」を1行ずつ完結させること
- **MFの勘定科目の自動提案（雲アイコン）は誤りが多い。必ず1件ずつ確認する**
  - 実例：光回線→雑費／オンラインサロン会費→接待交際費／AIサブスク→仕入高
  - 一度手で直して登録すると、次回から同じ摘要には正しい科目が提案される（学習する）

## 無料プランの制限

**無料プランは仕訳50件が上限。** 超えると登録時にアップグレード案内が出て1件も登録できなくなる。
年間の仕訳が50件を超える見込みなら、作業を始める前にプランを確認する。

## 紙のカード明細との使い分け

「重要な注意事項」にある「クレジットカード明細は仕訳対象から除外」は、
**紙・PDFの一覧形式の明細**を1件の領収書として扱わない、という意味。
連携で取り込まれた**1取引ずつの明細は、通常どおり1件ずつ仕訳する**。

## 外貨建てサブスクの扱い

海外サービス（USD建て等）は領収書がドル表記のため、**円換算額は領収書から確定できない**。
カード明細に出る**実際の円請求額**を使う。連携していれば自動で入るので、
外貨サブスクがある場合はカード連携を先に済ませたほうが正確かつ速い。
